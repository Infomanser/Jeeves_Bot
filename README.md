# 🎩 Jeeves Bot

Персональний кишеньковий асистент на Python + Termux.
Створений для керування сервером, особистими нотатками та дрібними життєвими задачами.

## 🚀 Основні функції

### 🧠 Інтелектуальний Нотатник
- **Голосові нотатки:** Розпізнавання мови через **Groq (Whisper)**.
- **AI-Асистент:** Автоматична суммаризація довгих думок та виділення суті через **Llama 3**.
- **Структурування:** Автоматичний підбір тегів та сортування по категоріях.
- **Пошук:** Зручна навігація по тегах через меню кнопок.

### 🛡 Безпека та Доступ
- **Локальна довіра:** Права доступу прив'язані до конкретних чатів (система "Свій/Чужий").
- **Ролі:** Root (Власник), Creator (Творець чату), Trusted (Довірений).
- **Авто-бекап:** Щоденне резервне копіювання бази даних з ротацією.

### 🛠 Інструменти та Termux
- **Моніторинг:** Статус системи, пам'ять, логи PM2.
- **Керування:** Рестарт сервісів, SSH тунелів.
- **Hardware:** Керування ліхтариком 🔦, TTS (Text-to-Speech) 🗣, пошук телефону.

### 🌤 Lifestyle
- **Календар подій:** Нагадування, масовий імпорт.
- **Інфо:** Погода (Open-Meteo), Стрічка новин (RSS).

---

## 🏗 Структура проекту

```text
Jeeves_Bot
├── config.py                # ⚙️ Головні налаштування (зчитує .env, змінні OWNER_ID, API ключів)
├── main.py                  # 🚀 Точка входу. Запуск бота, реєстрація роутерів (handlers)
├── requirements.txt         # 📦 Список бібліотек (aiogram, groq, requests тощо)
├── README.md                # 📖 Опис проекту (що це за бот)
├── ROAD_MAP.md              # 🗺 План розвитку та список виконаних задач
│
├── handlers/                # 🧠 МОЗОК (Обробники команд та повідомлень)
│   ├── __init__.py
│   ├── navigation.py        # 🧭 Навігація по меню (вхід у папки "Керування", "Інфо"...)
│   ├── notes.py             # 📝 База Знань: Голосові, FSM, Groq API, теги, права доступу
│   ├── hardware.py          # 💻 Залізо: Рестарти серверів, SSH, Тунелі, статус Termux
│   ├── lifestyle.py         # 🌤 Життя: Погода, Курс валют (планується), Розваги
│   ├── common.py            # ℹ️ Загальне: /start, /help, скасування дій
│   ├── admin.py             # 👮‍♂️ Адмінка: керування групами (бан/мут - в планах)
│   ├── owner.py             # 👑 Root-команди: специфічні дії тільки для тебе
│   └── public.py            # 📢 Для всіх: реакції на текст у публічних чатах
│
├── keyboards/               # 🎹 ІНТЕРФЕЙС (Кнопки та меню)
│   ├── __init__.py
│   ├── builders.py          # 🏗 Головний конструктор: будує Main Menu, папки, списки
│   └── calendar_kb.py       # 📅 Специфічна клавіатура для вибору дати (календар)
│
├── services/                # 🔌 СЕРВІСИ (Зовнішні підключення та API)
│   ├── __init__.py
│   ├── db_manager.py        # 🗄 Робота з SQLite (з'єднання, бекапи)
│   ├── termux_api.py        # 📱 Керування телефоном (Termux API: ліхтар, батарея, TTS)
│   ├── calendar_api.py      # 🗓 Логіка календаря
│   ├── weather_api.py       # 🌦 OpenWeatherMap API
│   └── news_api.py          # 📰 News API / RSS парсери
│
└── utils/                   # 🛠 ІНСТРУМЕНТИ (Допоміжні скрипти)
    ├── __init__.py
    ├── logger.py            # 📝 Налаштування логування
    ├── filters.py           # 🛡 Кастомні фільтри (IsAdmin, IsOwner)
    └── helpers.py           # 🧹 Дрібні функції
```

# ⚙️ Встановлення на Termux

1. Підготовка середовища:

```bash
pkg install python termux-api git
```

2. Завантаження:

```bash
git clone https://github.com/Infomanser/Jeeves_Bot.git
cd Jeeves_Bot
```

3. Залежності:

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

4. Налаштування: Створити файл `.env` (див. приклад у `config.py`):

```text
BOT_TOKEN=12345:YourToken
OWNER_ID=123456789
JEEVES_GROQ_KEY=gsk_...
```

5. Запуск:

```bash
python main.py
# Або через PM2:
pm2 start main.py --name Jeeves --interpreter venv/bin/python
```

